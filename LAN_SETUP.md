# تشغيل TeamTracker على شبكة محلية (LAN)

دليل تشغيلي لإعداد TeamTracker داخل شبكة واحدة (منزل أو مكتب) دون الاعتماد على خادم سحابي خارجي لمسار البيانات الأساسي.

الجمهور المستهدف: مسؤول تقنية أو صاحب عمل يرغب في تجربة النظام محليًا، دون معرفة مسبقة بتفاصيل المشروع.

---

## نظرة عامة

TeamTracker مكوّن من جزأين:

| المكوّن | الدور | أين يعمل |
|---------|--------|----------|
| **خادم الإدارة (Admin Server)** | المصادقة، لوحة التحكم، تنسيق الجلسات | جهاز واحد داخل الشبكة (جهاز المسؤول) |
| **تطبيق الموظف (Desktop Tracker)** | تتبّع النشاط وبث الشاشة عند الطلب | جهاز كل موظف |

على الشبكة المحلية يتصل تطبيق الموظف بخادم المسؤول عبر عنوان IP داخلي (مثل `192.168.1.10`) على المنفذ `3001`. طالما الأجهزة على نفس شبكة الواي فاي أو الشبكة السلكية، لا يلزم وجود VPS لهذه التجربة.

> **شرط أساسي:** جهاز المسؤول الذي يشغّل الخادم يجب أن يبقى قيد التشغيل ومتصلاً بالشبكة طوال مدة الاستخدام.

---

## تطبيق الموظف الجاهز (GitHub Releases)

الموظفون **لا يحتاجون** إلى استنساخ المستودع البرمجي ولا إلى تثبيت أدوات التطوير (`Node.js` / `pnpm`).

يُحمَّل التطبيق الرسمي الجاهز من صفحة الإصدارات:

**[GitHub Releases — TeamTracker](https://github.com/hamdymohamedak/TeamTracker/releases)**

يوفر المشروع حزم تثبيت لأنظمة **Windows** و**macOS** و**Linux**. الحزمة المطلوبة هي تطبيق **الموظف (Employee / Desktop Tracker)** وليس بالضرورة حزمة `desktop-admin`.

### إعداد الخادم الافتراضي في الإصدارات الرسمية

النسخ المنشورة على GitHub مضبوطة افتراضيًا على بيئة الإنتاج السحابية:

```text
https://tracker.hostly-eg.com
```

للتشغيل على شبكة محلية يجب عند التفعيل تحديد عنوان الخادم الداخلي صراحةً، على الشكل:

```text
http://<IP-جهاز-المسؤول>:3001
```

يتم ذلك بإحدى طريقتين:

1. إدخال **Setup Token** مع حقل **Server URL** داخل التطبيق، أو  
2. استخدام ملف تفعيل يحتوي على `setupToken` و`serverUrl` الصحيحين.

---

## مخطط الاتصال

```text
جهاز المسؤول
  ├── خادم TeamTracker (المنفذ 3001)
  ├── لوحة التحكم (Dashboard)
  └── عنوان مثال: 192.168.1.10

              ▲
              │  شبكة محلية واحدة (LAN / Wi‑Fi)
              ▼

جهاز الموظف
  ├── تطبيق TeamTracker من GitHub Releases
  ├── التفعيل: Setup Token + http://192.168.1.10:3001
  └── يظهر بحالة Online على لوحة التحكم
```

---

## المتطلبات

| المتطلب | التفاصيل |
|---------|-----------|
| شبكة مشتركة | جميع الأجهزة على نفس الراوتر أو نفس شبكة الواي فاي |
| جهاز مسؤول | يشغّل الخادم ويفتح لوحة التحكم |
| أجهزة موظفين | تثبّت تطبيق سطح المكتب من Releases فقط |
| منفذ مفتوح | TCP `3001` على جهاز المسؤول باتجاه الشبكة الداخلية |
| صلاحيات النظام | تسجيل الشاشة (Screen Recording / ما يعادله) على جهاز الموظف عند استخدام البث المباشر |

---

## القسم أ — إعداد جهاز المسؤول

### أ‑1. تشغيل الخادم

من جذر المشروع:

```bash
cd admin
pnpm install
pnpm run dev
```

انتظر حتى يؤكد السجل أن الخدمة تستمع على المنفذ **3001**.

> أثناء التطوير قد تعمل واجهة Vite على المنفذ `5174` بينما واجهة البرمجة (API) على `3001`.  
> تطبيقات الموظفين على الشبكة المحلية يجب أن تستخدم دائمًا: `http://<IP>:3001`

### أ‑2. معرفة عنوان IP المحلي

#### macOS

- المسار الرسومي: **System Settings → Network → Wi‑Fi → Details**  
- أو من الطرفية:

```bash
ipconfig getifaddr en0
```

إذا لم يُرجع نتيجة، جرّب `en1`.

#### Windows

من **Command Prompt** أو **PowerShell**:

```bat
ipconfig
```

اختر **IPv4 Address** ضمن محوّل الواي فاي أو الشبكة السلكية، مثل: `192.168.1.10`.

#### Linux

```bash
ip -4 addr show
```

أو:

```bash
hostname -I
```

العنوان المناسب عادةً يبدأ بـ `192.168.` أو `10.`.

### أ‑3. فتح لوحة التحكم

1. يُفضَّل: `http://<IP-المحلي>:3001`  
2. للتطوير على نفس الجهاز فقط: `http://localhost:5174`

بعدها سجّل الدخول بحساب المسؤول.

> عند إنشاء ملف تفعيل تلقائي من لوحة التحكم، يُفضَّل فتح اللوحة عبر عنوان الـ IP (وليس `localhost`) حتى يُكتب `serverUrl` بشكل صحيح للأجهزة الأخرى.

### أ‑4. السماح بالمنفذ 3001 عبر جدار الحماية

نفّذ هذه الخطوة إذا تعذّر اتصال أجهزة الموظفين بالخادم.

#### macOS

**System Settings → Network → Firewall** — اسمح لتطبيق Node أو الطرفية بقبول الاتصالات الواردة على الشبكة المحلية.

#### Windows

**Windows Defender Firewall → Advanced settings → Inbound Rules → New Rule**  
قاعدة لـ **TCP** على المنفذ **3001** مع السماح بالاتصال.

أو من PowerShell بصلاحيات مسؤول:

```powershell
New-NetFirewallRule -DisplayName "TeamTracker LAN" -Direction Inbound -Protocol TCP -LocalPort 3001 -Action Allow
```

#### Linux (ufw)

```bash
sudo ufw allow 3001/tcp
sudo ufw reload
```

### أ‑5. إضافة موظف وإنشاء رمز التفعيل

1. من لوحة التحكم: **Employees → Add Employee**  
2. افتح سجل الموظف → **Setup Token**  
3. انسخ الرمز وشاركه مع المستخدم عبر قناة آمنة داخل المؤسسة  

**اختياري — ملف التفعيل:**  
خيار **Install on this device** ينزّل ملفًا بالشكل `teamtracker-activate-….json`.  
تأكد أن الحقل `serverUrl` داخله يساوي `http://<IP-المحلي>:3001`. إذا فُتحت اللوحة عبر `localhost`، عدّل الملف يدويًا أو اطلب من الموظف إدخال عنوان الخادم داخل التطبيق.

---

## القسم ب — إعداد جهاز الموظف

### ب‑1. تنزيل التطبيق

من صفحة الإصدارات:

https://github.com/hamdymohamedak/TeamTracker/releases

| النظام | الحزمة المناسبة |
|--------|------------------|
| **Windows** | مثبت `.exe` لتطبيق الموظف |
| **macOS (Apple Silicon)** | إصدار **arm64** |
| **macOS (Intel)** | إصدار **x64** |
| **Linux** | **AppImage** أو حزمة **.deb** |

اختر حزمة **Employee / Desktop Tracker**. حزمة `desktop-admin` مخصّصة لصدفة إدارية وليست مطلوبة لكل موظف.

### ب‑2. التثبيت حسب نظام التشغيل

#### Windows

1. شغّل ملف التثبيت.  
2. عند ظهور **SmartScreen**: **More info → Run anyway**.  
3. افتح التطبيق وامنح أذونات الشاشة عند الطلب.

#### macOS

1. افتح الحزمة (`.dmg` أو التطبيق بعد فك الضغط).  
2. عند اعتراض Gatekeeper: انقر بالزر الأيمن → **Open** → أكّد **Open**.  
3. من **System Settings → Privacy & Security** فعّل:
   - **Screen Recording**
   - **Accessibility** (إن طُلب)
4. أعد تشغيل التطبيق بعد منح الأذونات.

#### Linux

**AppImage:**

```bash
chmod +x TeamTracker*.AppImage
./TeamTracker*.AppImage
```

**حزمة .deb:**

```bash
sudo dpkg -i teamtracker*.deb
```

وافق على طلب مشاركة الشاشة / PipeWire عند ظهوره.

### ب‑3. ربط الجهاز بالخادم المحلي

عند التشغيل لأول مرة:

1. الصق **Setup Token** الصادر من لوحة التحكم.  
2. في حقل الخادم (**Server URL** / الإعدادات المتقدمة) أدخل:

```text
http://192.168.1.10:3001
```

استبدل العنوان بـ IP جهاز المسؤول الفعلي.

3. أكمل التفعيل (**Connect**).

**بديل عبر ملف التفعيل:**  
ضع الملف `teamtracker-activate-….json` في مجلد **Downloads** قبل أول تشغيل، بالمحتوى التالي (بعد التعديل):

```json
{
  "setupToken": "<رمز-التفعيل>",
  "serverUrl": "http://192.168.1.10:3001"
}
```

عند فتح التطبيق يُسترد الرمز تلقائيًا إن وُجد الملف صالحًا.

### ب‑4. التحقق من الاتصال

على لوحة تحكم المسؤول يجب أن تظهر حالة الموظف **Online / متصل** خلال دقيقة تقريبًا.

---

## القسم ج — البث المباشر (Live Activity)

1. من لوحة التحكم افتح **Live Activity** (أو لوحة النشاط المباشر).  
2. اختر الموظف المتصل.  
3. اضغط **Start**.

على شبكة محلية مستقرة يُتوقَّع أحد المسارين:

| المسار | المعنى |
|--------|--------|
| **WebRTC · LAN** | اتصال نظير إلى نظير داخل الشبكة — أداء أعلى ومعدل إطارات أفضل |
| **Binary WebSocket** | مسار احتياطي عبر خادم المسؤول المحلي — أبطأ نسبيًا، وما يزال داخل الشبكة |

يبقى الخادم وأجهزة الموظفين على الشبكة نفسها طوال الجلسة.

---

## بناء مخصّص موجّه لخادم LAN (اختياري)

ليس إلزاميًا إذا أدخل الموظف `Server URL` يدويًا عند التفعيل.

لإنشاء نسخة من التطبيق تشير افتراضيًا إلى خادم الشبكة المحلية:

```bash
cd desktop
TEAMTRACKER_SERVER_URL=http://192.168.1.10:3001 pnpm run build
```

ثم وزّع مخرجات مجلد `release/` على أجهزة الموظفين.

الإصدارات العامة من GitHub تظل صالحة للشبكة المحلية طالما حُدِّد `serverUrl` الصحيح أثناء التفعيل.

---

## استكشاف الأعطال

| العَرَض | الإجراء المقترح |
|---------|------------------|
| الموظف لا يظهر Online | تحقق من صحة الـ IP، وتشغيل الخادم، ووجود المنفذ `3001` في العنوان |
| التطبيق يتصل بالسحابة بدل الشبكة المحلية | أدخل `http://<IP>:3001` صراحة عند التفعيل |
| البث المباشر أسود أو عالق على الاكتشاف | راجع أذونات تسجيل الشاشة ومسار WebRTC على جهاز الموظف |
| الجهاز خارج شبكة الموقع | الخادم المحلي غير متاح من الإنترنت العام — يلزم VPS أو VPN |
| تغيّر الـ IP بعد إعادة تشغيل الراوتر | حدّث `Server URL` أو أعد التفعيل بالعنوان الجديد |
| رفض الاتصال من أجهزة أخرى | افتح قاعدة جدار الحماية للمنفذ TCP `3001` (القسم أ‑4) |

---

## قائمة تحقق سريعة

**المسؤول**

1. تشغيل الخادم: `cd admin && pnpm run dev`  
2. تحديد عنوان IP المحلي  
3. فتح لوحة التحكم  
4. إنشاء موظف وإصدار Setup Token  

**الموظف**

1. تنزيل التطبيق من [Releases](https://github.com/hamdymohamedak/TeamTracker/releases)  
2. التثبيت وفق نظام التشغيل  
3. التفعيل: الرمز + `http://<IP-المسؤول>:3001`  
4. منح أذونات الشاشة  

**المسؤول — المتابعة**

5. التأكد من حالة Online ثم تشغيل Live Activity  

---

## مقارنة أوضاع النشر

| الوضع | عنوان الخادم | إعداد تطبيق الموظف |
|-------|--------------|---------------------|
| **إنتاج سحابي** | `https://tracker.hostly-eg.com` | إصدار Releases + Setup Token |
| **شبكة محلية (LAN)** | `http://<IP-المحلي>:3001` | نفس الإصدار + Setup Token **و** Server URL للعنوان المحلي |
| **استضافة خاصة (VPS)** | نطاق المؤسسة | إصدار Releases + Server URL للنطاق |

---

## مراجع ذات صلة

- [دليل النشر على VPS](./docs/DEPLOYMENT.md) — للاستخدام من خارج الشبكة المحلية  
- [Live View / WebRTC](./docs/live-view-webrtc.md) — تفاصيل مسار البث المباشر  

---

*هذا المستند يغطي التشغيل داخل شبكة واحدة. للمراقبة عن بُعد عبر الإنترنت العام يُرجى اتباع دليل النشر على خادم مستقل.*
