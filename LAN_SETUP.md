# تشغيل TeamTracker على شبكة محلية (LAN)

دليل تشغيلي لإعداد مكتب محلي دون الاعتماد على خادم سحابي لمسار البيانات الأساسي.

الجمهور المستهدف: صاحب عمل أو مدير يريد تشغيل النظام محليًا **بدون كود** وبدون إدخال عناوين IP يدويًا.

---

## الطريقة الموصى بها (بدون كود · اكتشاف تلقائي)

### نظرة عامة

| المكوّن | الدور | الحزمة من GitHub Releases |
|---------|--------|---------------------------|
| **TeamTracker Admin** | يشغّل الخادم + لوحة التحكم على جهاز المدير | `TeamTracker-Admin-…` |
| **TeamTracker** (الموظف) | تتبّع النشاط وبث الشاشة | `TeamTracker-…` |

عند فتح **Admin** المعبأ، يبدأ خادم محلي على المنفذ `3001`، ويُعلن المكتب على الشبكة عبر **mDNS** باسم المؤسسة (أو `TEAMTRACKER_OFFICE_NAME`). تطبيق الموظف يعرض قائمة **اختر مكتبك** بدون الحاجة لمعرفة الـ IP.

> **شرط أساسي:** جهاز المدير الذي يشغّل Admin يجب أن يبقى قيد التشغيل ومتصلاً بنفس شبكة الواي فاي / السلكية طوال مدة الاستخدام.

```text
جهاز المدير
  └── TeamTracker Admin (خادم مضمّن + لوحة التحكم)
        └── يعلن المكتب على الشبكة: «اسم المؤسسة»

              ▲  mDNS + TCP 3001
              │
جهاز الموظف
  └── TeamTracker → اختر المكتب → الصق Setup Token
```

### خطوات المدير

1. حمّل وثبّت **TeamTracker Admin** من [GitHub Releases](https://github.com/hamdymohamedak/TeamTracker/releases).
2. افتح التطبيق — ينتظر حتى يصبح الخادم المحلي جاهزًا ثم يعرض لوحة التحكم.
3. أنشئ حساب المؤسسة وأضف الموظفين.
4. من **Employees** سترى شريط **الشبكة المحلية** يوضح أن المكتب ظاهر بالاسم المختار.
5. لكل موظف: **Setup Token** — انسخ الرمز أو امسح/احفظ **QR** (نفس بيانات ملف التفعيل).
6. اترك Admin مفتوحًا (أو في شريط القوائم) أثناء عمل الفريق.

### خطوات الموظف

1. حمّل وثبّت تطبيق **TeamTracker** (الموظف) من نفس صفحة الإصدارات.
2. عند أول فتح: انتظر ظهور المكتب في **اختر مكتبك**، ثم حدّده.
3. الصق **Setup Token** واضغط Connect.
4. إن لم يظهر المكتب: افتح **إعدادات متقدمة** وأدخل عنوان الاحتياطي الظاهر في لوحة المدير، أو احفظ محتوى QR كملف `teamtracker-activate-*.json` في مجلد Downloads.

### الجدار الناري والشبكة

| المطلوب | التفاصيل |
|---------|----------|
| TCP `3001` | وارد على جهاز المدير (لوحة التحكم + API) |
| UDP `5353` | mDNS / Bonjour لاكتشاف اسم المكتب |
| نفس الشبكة | تجنّب Guest Wi‑Fi وعزل العملاء (AP isolation) |
| VPN | قد يمنع الاكتشاف؛ استخدم QR / العنوان الاحتياطي |

#### Windows (مثال)

```powershell
New-NetFirewallRule -DisplayName "TeamTracker LAN" -Direction Inbound -Protocol TCP -LocalPort 3001 -Action Allow
```

### تعطيل الاكتشاف على VPS عام

على خادم سحابي عام، عطّل الإعلان:

```bash
TEAMTRACKER_LAN_DISCOVERY=0
```

---

## بديل للمطوّرين (من الكود)

إذا كنت تطوّر من المستودع:

```bash
cd admin
pnpm install
pnpm run dev
```

الخادم يستمع على `0.0.0.0:3001` ويعلن mDNS افتراضيًا. لتشغيل Admin Electron مع خادم مضمّن أثناء التطوير:

```bash
cd desktop-admin
pnpm run dev:local
```

أو غلاف فقط يشير لخادم خارجي:

```bash
TEAMTRACKER_EMBED_SERVER=0 TEAMTRACKER_ADMIN_URL=http://localhost:3001 pnpm run dev
```

### معرفة عنوان IP يدويًا (احتياطي)

#### macOS

```bash
ipconfig getifaddr en0
```

#### Windows

```powershell
ipconfig
```

ابحث عن IPv4 لشبكة الواي فاي / الإيثرنت (مثل `192.168.1.10`).

الموظف يمكنه إدخال `http://<IP>:3001` من **إعدادات متقدمة** إن فشل الاكتشاف.

### ملف التفعيل

من لوحة الموظفين → **Install on this device** يُنزَّل JSON:

```json
{
  "setupToken": "…",
  "serverUrl": "http://192.168.1.10:3001",
  "employeeName": "…",
  "createdAt": "…"
}
```

`serverUrl` يُفضّل عنوان LAN من `/api/lan/info` حتى لو فُتحت اللوحة عبر `localhost`. ضع الملف في Downloads على جهاز الموظف قبل أول تشغيل.

---

## Live View على الشبكة المحلية

عند اتصال الموظف بنفس الشبكة، يمكن لمسار WebRTC · LAN أن يعمل نظيرًا إلى نظير لأداء أعلى. راجع [docs/live-view-webrtc.md](docs/live-view-webrtc.md).

---

## استكشاف الأخطاء

| العرض | ماذا تجرب |
|-------|-----------|
| الموظف لا يرى مكاتب | تأكد أن Admin يعمل؛ نفس الشبكة؛ لا عزل AP؛ جدار ناري UDP 5353؛ استخدم QR |
| Connect يفشل | رمز منتهي أو مستخدم؛ عنوان خاطئ؛ جدار ناري TCP 3001 |
| Admin لا يفتح اللوحة | راجع `local-server.log` داخل مجلد بيانات التطبيق؛ أعد تشغيل الخادم من القائمة |
| تريد سحابة بدل المحلي | من قائمة Admin: **Change Dashboard Mode…** وأشر لعنوان الإنتاج |

---

## متغيرات البيئة ذات الصلة

| المتغير | المعنى |
|---------|--------|
| `TEAMTRACKER_LAN_DISCOVERY` | `0` / `false` لتعطيل mDNS (الافتراضي: مفعّل) |
| `TEAMTRACKER_OFFICE_NAME` | اسم المكتب الظاهر للموظفين (وإلا اسم المؤسسة) |
| `TEAMTRACKER_EMBED_SERVER` | لـ desktop-admin: فرض/منع تضمين الخادم |
| `PORT` | منفذ الاستماع (افتراضي `3001`) |
